import React, {useEffect, useState} from 'react';
import {
  StyleSheet,
  View,
  Text,
  TouchableOpacity,
  ScrollView,
} from 'react-native';
import {Section, TableView} from 'react-native-tableview-simple';

const AddDeptScreen = () => {
  return (
    <ScrollView
      showsVerticalScrollIndicator={false}
      alwaysBounceVertical={false}>
      <Text>하이</Text>
    </ScrollView>
  );
};

export default AddDeptScreen;
